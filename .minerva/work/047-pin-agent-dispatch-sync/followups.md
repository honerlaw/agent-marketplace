# Followups: pin-agent-dispatch-sync

Deferred work surfaced during unit 047 that was out of scope for this run.

- **Re-derive `minerva:ship`'s CI-watch polling cadence and its rationale.** `plugins/minerva/skills/ship/references/protocol.md` (line ~89) justifies `ScheduleWakeup` at `delaySeconds: 270` as *"stays under the 5-minute prompt-cache TTL — see ScheduleWakeup docs"*. Current tool guidance contradicts the premise: this session type uses a 1-hour prompt-cache TTL, the tool explicitly warns against short-interval polling for harness-tracked work, and it recommends matching the delay to how fast the watched state actually changes (with 1200s+ for fallback heartbeats). Under a 1-hour TTL the sentence is not merely stale but near-vacuous — almost any legal interval satisfies it — so the fix is to re-derive the cadence from observed CI durations and restate *why* that number, not to swap one figure for another. **Why deferred:** both propose-phase reviewers flagged it as a different defect class from 047's (it touches no dispatch site), and bundling it would have made the diff harder to audit against the stated goal. The same pass should sweep the Phase 7 cleanup-gate `delaySeconds: 300` in all three autonomous orchestrators, which carry the same assumption.

- **Phase 4.5 is told to log to a scratchpad Phase 4 just archived.** All three autonomous orchestrators (`propose-ship-auto`, `-balanced`, `-quick`) order promote (Phase 4, which archives `scratchpad.md` and replaces it with the one-line post-promote marker) *before* synthesis (Phase 4.5, whose instruction is "log the outcome with one line under `## Balanced decisions`" in `scratchpad.md`). By then that file is the canonical empty marker, which [[003-constraint-post-promote-scratchpad-canonical-empty]] says downstream skills depend on, so the line has nowhere legitimate to go. Unit 047 appended it to `archive/scratchpad.md` instead. Fix by naming the archive explicitly as the Phase 4.5 log destination, or by moving the synthesis step ahead of the scratchpad archival. **Why deferred:** it is a defect in the orchestrators' own phase text rather than in this unit's subject, and touching the running orchestrator's protocol mid-run is what the out-of-scope rule warns against.

- **Consider whether `review`'s local-diff dispatch should also pin `subagent_type` / `model`.** Unit 047 added only `run_in_background: false` to `plugins/minerva/skills/review/references/protocol.md`; unlike the other four dispatch sites it pins no agent type or model tier at all, so both are left to the model's discretion. Pinning them would make review-mode cost deterministic the way `round-table`'s and `propose-ship-balanced`'s panelist pins do. **Why deferred:** choosing a tier for a code-quality review is an authoring decision with its own tradeoff (depth vs. cost), not a mechanical extension of this unit's invariant.
