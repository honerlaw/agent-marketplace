# Scratchpad: init-routing-wiki-protocol (029)

## Panel decisions 2026-06-03

- [2/3 accept → revise → 3/3 accept] scope check: ONE unit (template + refresh + contract
  + dogfood are four faces of one reading-protocol change; refresh discharges init's
  recorded L183 "--refresh out of scope for v1" deferral). Skeptic pin-downs folded:
  exact template + detection arithmetic; gate framed "may be customized, not just stale";
  replace-vs-splice decided (gated whole-section replace w/ diff + removal warning,
  splice = future hardening); router-not-methodology bullet altitude.
- [2/3 accept → revise → 3/3 accept] approach (A′ over B template-only / C auto-rewrite):
  folds — staleness markers GENERIC from the template-of-record (disjunction, opposite
  quantifier from detection's conjunction); `^## `-or-EOF boundary w/ worked example
  (### non-terminating); idempotency round-trip criterion; anchors `overview.md` +
  "you may have customized it" (not bare `refresh`); per-detected-agent-file; Step-4
  trigger widened to created-or-refreshed; L183 deleted; unit renumbered 029 (028 taken
  by knowledge entry; 029 restores unit↔entry parity).
- [2/3 accept → revise → 3/3 accept] whole-proposal (v2): byte-for-byte template +
  exact anchor literals embedded in the proposal; criterion-5 verification pinned as
  by-inspection against the L137 detection rule; L183 = bare deletion + positive Step-3
  description + two new out-of-scope bullets. Arbiter verified the embedded template
  keeps every existing contract anchor + re-detects inside the 6-line window.

## Notes
