# Scratchpad: intake-matches-open-issues

## Balanced decisions 2026-08-22
- [reviewed — clean] scope check: single unit, 8 file groups (Skeptic accept; flagged handoff idempotency, explore's tonal fit and the match-fuzziness bar as drafting requirements, not scope gaps — all three folded into the approach)
- [reviewed — folded] approach: option A (one canonical reference + qualified citations) over B (inline copies, drift) and C (new skill, four catalog surfaces). Skeptic surfaced two load-bearing defects, both verified against the corpus and folded: (1) D7 claimed the in-flight-collision precedent does not increment the escalation counter — it does ("every user escalation (predicate-driven or hardcoded)"), so the issue-match ask increments like every other hardcoded trigger, no exception; (2) the pointer-integrity tests read only SKILL.md, so this unit's own qualified citations inside phases.md would be unguarded — the guard is extended to references/*.md, which immediately caught four pre-existing dangling pointers now in scope to qualify.
- [decided] whole-proposal soundness: single surface (skill prose + one test module), no public interface, no knowledge-constraint conflict (solo gate)
- [reviewed — clean] completion verification: Verifier accept — reproduced all 9 criteria, mutation-tested the new negative coverage (injected a dangling pointer → fails; narrowed `skill_docs` back to SKILL.md → fails), confirmed 653 passed. Noted-but-dismissed: Step 1 names the `gh repo view` command inline while citing promote's heading; kept deliberately — the *outcome* differs (skip silently vs. take the file path), and promote's decision tree (viewerPermission caveat, nameWithOwner recording) is not copied.
- [decided] review triage (solo): 8 findings — 2 high FIX (via replan), 1 medium FIX, 2 low FIX, 2 low already fixed, 1 low IGNORE (finding 6: quoting a heading by prefix is what `unresolved_heading_citations` documents as correct, not a defect)
- [reviewed — folded] replan-vs-FIX: replan. `minerva:review`'s "Load-bearing divergence" heuristic lists "scope shifted in or out → replan" with no size exception; Skeptic accepted and independently confirmed `modes.md` was untouched by the diff. It also caught that my artifact quoted an "OUT OF SCOPE" sentence the proposal does not literally contain — the boundary was implicit in the eight Approach steps and `## Why`; the replan entry says that instead.
- [reviewed — folded] new-plan acceptance: Skeptic returned `revise` on two verified load-bearing gaps, both folded. (1) The plan patched `minerva:promote` only, but `minerva:ship` explicitly permits shipping without promote ("the user can ship and skip") and reads `**Closes**` mechanically — so `ship/references/protocol.md` came into scope too; it bites hardest in the autonomous orchestrators, which auto-accept ship's PR-body gate. (2) The "linked, not adopted" record was a scratchpad line, which promote Mode A's four-way partition would most naturally DISCARD (its only documented exemption is the `→ promoted to` marker) — replaced with a `**Linked**` proposal field, read directly, written where `on-approval.md` already creates the file.
- [process note] the Phase 3 code-quality reviewer was dispatched with `model: sonnet`; `minerva:review`'s "Code review invocation" says to leave the model unpinned so it inherits the session tier. Deviation recorded rather than re-run — the pass returned two high findings that both survived independent verification.
