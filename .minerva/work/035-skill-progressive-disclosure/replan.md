# Replan log: skill-progressive-disclosure

## 2026-06-11 — CI enumeration gap (completion-panel finding)

**Original plan.** Approach step 3: a new enumerating test (`tests/test_skill_budget.py`) enforces the 9KB byte budget and bidirectional reference-pointer integrity; success criterion 1 claims the cap is "enforced by the new test in CI". Implicit assumption: adding the test module suffices for CI enforcement.

**What changed.** The completion-verification panel (1/3 accept) surfaced that `.github/workflows/evals.yml` runs an explicitly enumerated test list — bare `pytest` aborts collection on pre-existing broken financials modules — so a new test module does not run in CI until appended to that list. (The workflow's scope note says, verbatim, "this job runs exactly the tests these eval mechanisms own"; that new modules must be appended by hand is the direct inference from it.) Also surfaced: the ephemeral completion checklist overcounted anchor retargets (claimed 26; mechanical count 25; no durable file carried the wrong number — this entry is the durable correction); one of round-table's 14 changed lines is a sanctioned input-scope narrowing (CONTEXT limited to knowledge entries already cited in session, per approved Approach item 4), not pure layout, and the scratchpad audit note mislabeled it; `evals/README.md`'s illustrative anchor pointed at `references/partition.md`, which exists in no skill, in the very section stating the runner fails on dangling `file` targets; and two success criteria overstate their evidence — criterion 4 says pointer integrity is test-enforced "from a specific step" while the test checks mention-anywhere (the read-before-step pointer style is real in all nine cores but spot-checked, not test-enforced), and criterion 2's "full pytest suite is green" elides three financials modules broken on main from the same root cause (the deleted financials tooling: `test_browser.py` and `test_storage.py` abort collection importing the deleted `lib/`; `test_pull.py` collects, then fails at runtime on the deleted `pull` module).

**New plan.** (a) Append `tests/test_skill_budget.py` to the pytest enumeration in `.github/workflows/evals.yml`. (b) Fix `evals/README.md`'s illustrative anchor to a self-consistent real path for its declared skill (`propose`): `{"any_of": ["EnterWorktree"], "file": "references/on-approval.md"}`. (c) Amend proposal success criterion 4 to claim exactly what the test enforces (every reference mentioned by name in its core and every mention resolving, both directions; read-before-step style noted as spot-checked), and criterion 2 to scope "suite green" as excluding the three pre-existing-broken financials modules. (d) Correct the scratchpad audit note on the round-table narrowing. (e) Re-run completion verification. Goal and approach otherwise unchanged.
