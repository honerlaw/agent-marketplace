# Dummy, non-secret values that let the image start for CI's /healthz smoke
# test (.github/workflows/mcp.yml). With no GSC_CREDENTIALS_JSON the server
# falls back to Application Default Credentials, resolved only when a tool runs.
MCP_AUTH_TOKEN=ci
